---
layout: page
title: HubSpot DevTools for Chrome and Firefox
desc: A browser extension for HubSpot CMS developers with quick access to debug parameters and HubL documentation.
permalink: /hubspot-devtools/
---

{# Hero Section #}
<section class="text-center">
  <h1 class="m-0 max-w-4xl mx-auto text-balance">
    HubSpot DevTools for Chrome and Firefox
  </h1>
  <p class="max-w-2xl mx-auto mt-6 text-balance text-lg text-neutral-600">
    A browser extension for HubSpot CMS developers. Quick access to debug parameters, cache busting, and HubL documentation — all from your browser toolbar.
  </p>

  {# Download Buttons #}
  <div class="flex gap-4 flex-col sm:flex-row justify-center items-center mt-8 md:mt-12">
    <a href="https://chromewebstore.google.com/detail/jmlcckldioeofjchfaomgaimhpnlfgok" class="btn btn--primary w-full sm:w-auto">
      {% phicon "google-chrome-logo", "duotone", { class: "size-6" } %}
      <span>Add to Chrome</span>
    </a>
    <a href="https://addons.mozilla.org/en-US/firefox/addon/freshjuice-hubspot-devtools/" class="btn btn--secondary w-full sm:w-auto">
      {% phicon "puzzle-piece", "duotone", { class: "size-6" } %}
      <span>Add to Firefox</span>
    </a>
  </div>

  <p class="my-4">— or —</p>
  <p class="mt-4">
    <small>
      <em>
        <a href="https://github.com/freshjuice-dev/hubspot-devtools-extension" class="inline-flex items-center justify-center gap-1.5 hover:text-red-600">
          Squeeze your own juice from the source code
          {% phicon "github-logo", "regular", { class: "size-4" } %}
        </a>
      </em>
    </small>
  </p>

  {# Privacy Note #}
  <div class="mt-12 md:mt-14 w-fit mx-auto flex items-center gap-3 px-4 py-3 bg-green-50 border border-green-200 rounded-lg text-sm">
    {% phicon "shield-check", "duotone", { class: "size-5 text-green-600 shrink-0" } %}
    <p class="m-0 text-neutral-600">
      <strong class="text-green-700">Privacy First:</strong> No data collection, no tracking, no telemetry.
      <a href="https://github.com/freshjuice-dev/hubspot-devtools-extension/blob/main/PRIVACY.md" class="text-green-700 hover:text-green-900 underline underline-offset-2">Learn more</a>
    </p>
  </div>
</section>

{# Screenshot Comparison Slider #}
<section class="mt-16 md:mt-24">
  <div
    class="max-w-2xl mx-auto rounded-2xl overflow-hidden shadow-lg border-2 border-zinc-200 select-none"
    x-data="{
      position: 71,
      container: null,
      init() {
        this.container = this.$el;
      },
      startDrag() {
        window.addEventListener('mousemove', this.onDrag);
        window.addEventListener('mouseup', this.stopDrag);
        window.addEventListener('touchmove', this.onDrag);
        window.addEventListener('touchend', this.stopDrag);
      },
      stopDrag() {
        window.removeEventListener('mousemove', this.onDrag);
        window.removeEventListener('mouseup', this.stopDrag);
        window.removeEventListener('touchmove', this.onDrag);
        window.removeEventListener('touchend', this.stopDrag);
      },
      onDrag(e) {
        const rect = this.container.getBoundingClientRect();
        const x = (e.touches ? e.touches[0].clientX : e.clientX) - rect.left;
        this.position = Math.max(0, Math.min(100, (x / rect.width) * 100));
      }
    }"
    x-init="init(); onDrag = onDrag.bind($data); stopDrag = stopDrag.bind($data)"
    @mousedown="startDrag()"
    @touchstart="startDrag()"
  >
    <div class="relative aspect-video cursor-ew-resize">
      {# Light mode image (background/right side) #}
      <img
        src="./light-mode.png"
        alt="HubSpot DevTools - Light Mode"
        class="absolute inset-0 w-full h-full object-cover object-left-top pointer-events-none"
        draggable="false"
      >

      {# Dark mode image (foreground/left side with clip) #}
      <div
        class="absolute inset-0 overflow-hidden pointer-events-none"
        :style="'clip-path: inset(0 ' + (100 - position) + '% 0 0)'"
      >
        <img
          src="./dark-mode.png"
          alt="HubSpot DevTools - Dark Mode"
          class="absolute inset-0 w-full h-full object-cover object-left-top"
          draggable="false"
        >
      </div>

      {# Slider handle #}
      <div
        class="absolute top-0 bottom-0 w-1 bg-orange-500 shadow-lg -translate-x-1/2"
        :style="'left: ' + position + '%'"
      >
        {# Handle knob #}
        <div class="absolute top-1/2 left-1/2 -translate-x-1/2 -translate-y-1/2 size-10 rounded-full bg-orange-500 border-4 border-white shadow-lg flex items-center justify-center">
          {% phicon "arrows-out-line-horizontal", "bold", { class: "size-5 text-white" } %}
        </div>
      </div>

      {# Labels #}
      <div class="absolute top-3 left-3 px-2 py-1 bg-zinc-900/80 text-white text-xs font-medium rounded">
        Dark
      </div>
      <div class="absolute top-3 right-3 px-2 py-1 bg-white/90 text-zinc-900 text-xs font-medium rounded shadow">
        Light
      </div>
    </div>
  </div>
  <p class="text-center text-sm text-zinc-500 mt-4">
    Drag to compare dark and light themes
  </p>
</section>

{# Features Section #}
<section class="mt-16 md:mt-24">
  <h2 class="text-center mb-12">
    Built for HubSpot Developers
  </h2>

  <div class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
    {# Feature 1 #}
    <div class="p-6 bg-zinc-100 border-2 border-zinc-200 rounded-2xl">
      <div class="size-12 rounded-full bg-orange-100 flex items-center justify-center mb-4">
        {% phicon "bug", "duotone", { class: "size-6 text-orange-600" } %}
      </div>
      <h3 class="text-lg font-semibold m-0 mb-2">Debug Mode Toggle</h3>
      <p class="text-neutral-600 m-0 text-sm">
        Quickly add <code class="text-xs bg-zinc-200 px-1.5 py-0.5 rounded">hsDebug=true</code> to your current page with one click.
      </p>
    </div>

    {# Feature 2 #}
    <div class="p-6 bg-zinc-100 border-2 border-zinc-200 rounded-2xl">
      <div class="size-12 rounded-full bg-orange-100 flex items-center justify-center mb-4">
        {% phicon "arrows-clockwise", "duotone", { class: "size-6 text-orange-600" } %}
      </div>
      <h3 class="text-lg font-semibold m-0 mb-2">Cache Buster</h3>
      <p class="text-neutral-600 m-0 text-sm">
        Add <code class="text-xs bg-zinc-200 px-1.5 py-0.5 rounded">hsCacheBuster</code> timestamp to bypass HubSpot caching instantly.
      </p>
    </div>

    {# Feature 3 #}
    <div class="p-6 bg-zinc-100 border-2 border-zinc-200 rounded-2xl">
      <div class="size-12 rounded-full bg-orange-100 flex items-center justify-center mb-4">
        {% phicon "gear", "duotone", { class: "size-6 text-orange-600" } %}
      </div>
      <h3 class="text-lg font-semibold m-0 mb-2">BackOffice Dev Mode</h3>
      <p class="text-neutral-600 m-0 text-sm">
        Enable <code class="text-xs bg-zinc-200 px-1.5 py-0.5 rounded">developerMode=true</code> for HubSpot backend debugging.
      </p>
    </div>

    {# Feature 4 #}
    <div class="p-6 bg-zinc-100 border-2 border-zinc-200 rounded-2xl">
      <div class="size-12 rounded-full bg-orange-100 flex items-center justify-center mb-4">
        {% phicon "link", "duotone", { class: "size-6 text-orange-600" } %}
      </div>
      <h3 class="text-lg font-semibold m-0 mb-2">Auto-apply to Links</h3>
      <p class="text-neutral-600 m-0 text-sm">
        Automatically propagate debug parameters to all internal links on the page.
      </p>
    </div>

    {# Feature 5 #}
    <div class="p-6 bg-zinc-100 border-2 border-zinc-200 rounded-2xl">
      <div class="size-12 rounded-full bg-orange-100 flex items-center justify-center mb-4">
        {% phicon "book-open-text", "duotone", { class: "size-6 text-orange-600" } %}
      </div>
      <h3 class="text-lg font-semibold m-0 mb-2">HubL Quick Links</h3>
      <p class="text-neutral-600 m-0 text-sm">
        Fast access to HubL documentation — Functions, Variables, Filters, Loops, If Statements, and HubDB.
      </p>
    </div>

    {# Feature 6 #}
    <div class="p-6 bg-zinc-100 border-2 border-zinc-200 rounded-2xl">
      <div class="size-12 rounded-full bg-orange-100 flex items-center justify-center mb-4">
        {% phicon "layout", "duotone", { class: "size-6 text-orange-600" } %}
      </div>
      <h3 class="text-lg font-semibold m-0 mb-2">Design Manager</h3>
      <p class="text-neutral-600 m-0 text-sm">
        One-click access to HubSpot Design Manager from any page on your portal.
      </p>
    </div>
  </div>
</section>

{# Contributing Section #}
<section class="mt-16 md:mt-24 text-center">
  <h2 class="mb-4">
    Open Source & Community Driven
  </h2>
  <p class="max-w-2xl mx-auto text-neutral-600 mb-8">
    HubSpot DevTools is free, open-source, and built with love. We welcome contributions, bug reports, and feature suggestions from the community.
  </p>

  <div class="flex flex-wrap gap-4 justify-center">
    <a href="https://github.com/freshjuice-dev/hubspot-devtools-extension/issues/new?template=bug_report.md" class="btn btn--secondary btn--sm">
      {% phicon "bug-beetle", "regular", { class: "size-5" } %}
      Report a Bug
    </a>
    <a href="https://github.com/freshjuice-dev/hubspot-devtools-extension/issues/new?template=feature_request.md" class="btn btn--secondary btn--sm">
      {% phicon "lightbulb", "regular", { class: "size-5" } %}
      Request Feature
    </a>
    <a href="https://github.com/freshjuice-dev/hubspot-devtools-extension?tab=readme-ov-file#contributing" class="btn btn--secondary btn--sm">
      {% phicon "git-pull-request", "regular", { class: "size-5" } %}
      Contribute
    </a>
  </div>
</section>
