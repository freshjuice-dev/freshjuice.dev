---
permalink: /tags/
layout: page
title: Tags
excludeFromSitemap: true
noindex: true
---

<nav class="max-w-3xl mx-auto w-full min-h-9 leading-none">
  <a class="inline-flex items-center gap-2 group no-underline" href="/blog/">
    {% phicon "arrow-left", "bold", { class: "size-5 group-hover:-translate-x-2 transition-transform" } %}
    <span>Back to Blog</span>
  </a>
</nav>

<div class="prose max-w-3xl mx-auto">
  <h1>{{ title }}</h1>
</div>

<div class="prose max-w-3xl mx-auto flex flex-wrap justify-between items-center gap-y-2 gap-x-4">
  {% for tag in collections.categories %}
    {% if tag != 'index' %}
      {% set count = collections.posts | getFilteredByTag(tag) | length %}
      {% set size = 0.8 + (count * 0.125) %}
      {% if size > 1.8 %}{% set size = 1.8 %}{% endif %}
      <span>
        <a href="/tags/{{ tag | slugify }}/"
           class="post-tag"
           style="font-size: {{ size | round(2) }}em;">
          {{ tag }} ({{ count }})
        </a>
      </span>
    {% endif %}
  {% endfor %}
</div>
